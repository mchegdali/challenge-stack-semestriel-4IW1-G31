{% extends 'base.html.twig' %}

{% block title %}Demande de réinitialisation de mot de passe{% endblock %}

{% block body %}
<div class="dark:bg-black items-center h-screen flex justify-center sm:bg-gray-50">
<div class="p-3 bg-white shadow-md rounded-xl w-full flex items-center flex-col gap-3">
    <h1 class="font-bold text-3xl md:text-4xl mb-8">Demande de réinitialisation de mot de passe</h1>
    <div class="w-full max-w-xs">
        {{ form_start(requestForm, {'attr': {'class': 'space-y-4'}}) }}
            {{ form_row(requestForm.email, {'attr': {'class': 'block w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 sm:text-sm dark:bg-transparent dark:focus:ring-primary/25 dark:focus:border-primary/25'}}) }}
        {{ form_end(requestForm) }}
    </div>
</div>
</div>
{% endblock %}