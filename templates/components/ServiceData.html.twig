<div class="bg-white p-6 mx-6 my-2 rounded-lg flex flex-col">
	<div class='flex gap-2 justify-between items-center'>
		<div>
			<p class=''>Nom
				{{ service.name }}</p>
			<p class=''>Prix
				{{ service.price }} €</p>
				<p class=''>Taxe de
				{{ service.tax.value }}%</p>
		</div>
		<div>
			<a href="{{ path('update_service', {'id': service.id}) }}" class='text-darkPurple'>
				<i class="material-icons">edit</i>
			</a>
			<a href="{{ path('delete_service', {'id': service.id}) }}" class='text-red-500'>
				<i class="material-icons">delete</i>
			</a>
		</div>
	</div>
</div>
